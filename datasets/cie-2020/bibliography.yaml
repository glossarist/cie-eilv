---
# CIE S 017:2020 ILV — bibliographic references extracted from concept notes.
# Currently empty — all "Reference:" lines from upstream term pages point at
# the standard itself (already encoded as the authoritative source on each
# concept), so there are no separate bibliography entries to enumerate.
